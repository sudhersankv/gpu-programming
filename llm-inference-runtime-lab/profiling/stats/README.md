# nsys stats CSVs

Small CSV exports from `cache_on_warm.nsys-rep` / `cache_off_warm.nsys-rep`
(via `scripts/export_nsys_stats.py`). Used to build `docs/data.json` for the
GitHub Pages dashboard. Binary `.nsys-rep` / `.sqlite` stay gitignored.
