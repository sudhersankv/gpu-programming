# Client benchmark record (warm cache A/B)

Source of truth for Request 1 / Request 2 TTFT, E2E, and tok/s used by the
GitHub Pages dashboard. Derived speedups are computed in
`scripts/build_apc_dashboard_data.py` — do not hard-code them in `docs/index.html`.
